#!/usr/bin/env bash
set -e

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks not installed. Please install gitleaks to run secret scan." >&2
  exit 1
fi

gitleaks protect --staged --no-banner --redact
