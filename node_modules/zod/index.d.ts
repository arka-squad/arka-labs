export const z: {
  object: <T>(shape: { [K in keyof T]: { parse(v: any): T[K] } }) => { parse(data: any): T };
  string: () => {
    min: (len: number) => { parse(v: any): string };
    parse: (v: any) => string;
  };
};
